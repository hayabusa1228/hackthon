// Header.jsx
import React from 'react';

const Header = () => {
  return (
    <header className="header">
      {/* 左端にアプリケーション名、右側にアイコンやボタンを置くことも可能です */}
      <h1 className="headerTitle">My Zoom App</h1>
    </header>
  );
};

export default Header;
